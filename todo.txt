- add clicked css on icons (dotted border around)
- if the empty is empty, dont push anything
- resizing the start menu clicked panel
- restructure code
- if the desktop app item is already opened, cannot push again into startmenu, or have active app menu
- style _ in activeApp nav
- positioning elements randomly works, however when another element is passed through, all the elements shift